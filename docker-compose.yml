services:
  backend:
    build: .
    container_name: loadsense-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      PYTHONPATH: /app
    restart: unless-stopped
