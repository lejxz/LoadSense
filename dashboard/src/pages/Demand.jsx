import React,{useEffect,useState}from'react';
import{Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';
import{API}from'../App';
const colorFor=v=>v<=5?'#22c55e':v<=10?'#eab308':'#ef4444';
export default function Demand(){const[route,setRoute]=useState('ayala-sm-carbon');const[forecast,setForecast]=useState([]);useEffect(()=>{fetch(`${API}/api/demand/${route}`).then(r=>r.json()).then(d=>setForecast(d.forecast.map(x=>({...x,label:new Date(x.ds).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})})))).catch(()=>setForecast([]))},[route]);return <><div className="topbar"><div><h1>Demand</h1><p>Six-hour synthetic forecast with rush-hour peaks.</p></div><select value={route} onChange={e=>setRoute(e.target.value)}><option value="ayala-sm-carbon">Ayala-SM-Carbon</option><option value="colon-talamban">Colon-Talamban</option><option value="basak-pardo">Basak-Pardo</option></select></div><section className="panel chart"><ResponsiveContainer><BarChart data={forecast}><CartesianGrid stroke="rgba(255,255,255,.08)"/><XAxis dataKey="label" stroke="#9fb3c8"/><YAxis stroke="#9fb3c8"/><Tooltip contentStyle={{background:'#081421',border:'1px solid #1f344a'}}/><Bar dataKey="yhat" fill="#00C9A7" shape={props=><rect {...props} fill={colorFor(props.payload.yhat)} rx="3"/>}/></BarChart></ResponsiveContainer></section></>}
