import React from 'react';
import { CircleMarker, MapContainer, Popup, TileLayer } from 'react-leaflet';
import JeepneyCard from '../components/JeepneyCard';
const colors = { GREEN: '#22c55e', YELLOW: '#eab308', RED: '#ef4444', BLINKING_RED: '#ef4444' };
export default function FleetMap({ fleet }) { return <><div className="topbar"><div><h1>Fleet Map</h1><p>Live jeepney occupancy and movement across Cebu City.</p></div><span className="badge">{fleet.length} vehicles</span></div><div style={{display:'grid',gridTemplateColumns:'1fr 430px',gap:16}}><div className="map-wrap"><MapContainer center={[10.3157,123.8854]} zoom={13}><TileLayer attribution="&copy; OpenStreetMap" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{fleet.map(v=><CircleMarker key={v.vehicle_id} center={[v.lat,v.lon]} radius={11} pathOptions={{color:colors[v.occupancy_tier],fillColor:colors[v.occupancy_tier],fillOpacity:.85}}><Popup><b>{v.vehicle_id}</b><br/>{v.passenger_count}/16 passengers<br/>{v.occupancy_tier}<br/>{v.speed_kph} kph</Popup></CircleMarker>)}</MapContainer></div><section className="panel grid">{fleet.map(v=><JeepneyCard key={v.vehicle_id} vehicle={v}/>)}</section></div></>; }
