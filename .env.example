# Copy this file to `.env` and fill in secrets before running demo
# DO NOT commit `.env` to version control

# Optional LLM API key (used if you enable chatbot features)
GEMINI_API_KEY=

# Local server override (usually not needed)
API_URL=http://localhost:8000

# Other optional keys (examples)
# SOME_SERVICE_API_KEY=
